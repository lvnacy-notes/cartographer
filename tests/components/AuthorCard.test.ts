import { AuthorCard } from '../../src/components/AuthorCard';
import {
    catalogItems,
    catalogSchema,
    defaultSettings
} from '../fixtures';

describe('AuthorCard', () => {
  it('renders scaffold', () => {
    // ...scaffold: basic render test...
  });
});
