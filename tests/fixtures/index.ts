/**
 * Test fixtures barrel export.
 * Centralizes imports from all fixture files for easy access in tests.
 */

export { catalogSchema } from './catalogSchema';
export { catalogItems } from './catalogItems';
export { defaultSettings } from './defaultSettings';
